version: "3.9"
   
services:
  backend:
    build: 
     context: ./backend/
     dockerfile: ./Dockerfile
    environment:
      - FLASK_ENV=development
  frontend:
    build:
      context: ./frontend/
      dockerfile: ./Dockerfile
    ports:
      - 80:80
    depends_on:
      - backend

